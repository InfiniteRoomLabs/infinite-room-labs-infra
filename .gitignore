# Added by Spec Kitty CLI (auto-managed)
.codex/
.opencode/
.windsurf/
.gemini/
.cursor/
.qwen/
.kilocode/
.augment/
.roo/
.amazonq/
.github/copilot/
.kittify/.dashboard

# Terraform
.terraform/
*.tfstate
*.tfstate.backup
*.tfplan
crash.log
crash.*.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Terragrunt
.terragrunt-cache/

# Terragrunt-generated files (recreated on init)
backend.tf
providers.tf

# Environment variables (legacy -- superseded by fnox; kept ignored for safety)
.env
.env.*
!.env.example

# fnox local overrides (gitignored; encrypted project fnox.toml IS committed)
fnox.local.toml
fnox.*.local.toml

# Ansible vault password (legacy repo-local secret file -- now via fnox)
.vault-password

# Ansible Vault (encrypted secrets -- use vault.yml.example as template)
ansible/inventory/group_vars/all/vault.yml

# Ansible runtime
ansible/.venv/
ansible/facts_cache/

# Locally installed Galaxy collections (ansible.cfg collections_paths) -- vendor code, never committed
ansible/collections/

# Packer build artifacts (multi-GB qcow2 images) and download cache
packer/*/output/
packer/*/packer_cache/
packer_cache/
