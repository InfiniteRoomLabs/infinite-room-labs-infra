# {{ ansible_managed }}
# Global HTTP-to-HTTPS redirect middleware
apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: redirect-to-https
  namespace: {{ irl_k3s_namespace }}
spec:
  redirectScheme:
    scheme: https
    permanent: true
